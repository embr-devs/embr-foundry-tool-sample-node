# Optional: lock the OpenAPI/MCP routes behind a shared header.
# When TOOL_API_KEY is unset, both surfaces accept anonymous calls (handy for
# local development and the Foundry import wizard). When set, /api/* requests
# must include `x-api-key: <value>`.
TOOL_API_KEY=
